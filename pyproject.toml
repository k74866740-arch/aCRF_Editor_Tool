[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "acrf-editor-tool"
version = "1.0.0"
description = "A professional aCRF editor tool."
readme = "README.md"
dependencies = [
    "pymupdf>=1.24.0",
    "customtkinter>=5.2.0",
]
